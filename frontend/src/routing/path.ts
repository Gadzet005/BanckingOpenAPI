export enum Path {
    homePage = "/",
    loginPage = "/login",
    registerPage = "/register",
    accountPage = "/account",
    transferStatistics = "/transfers-statistics",
    transfers = "/transfers",
    bankAccounts = "/bank-accounts",
}
